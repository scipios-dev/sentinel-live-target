{
  "name": "sentinel-live-target",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Disposable Sentinel pr-strategy live-run target: one deliberately-outdated published dependency (kleur, one patch behind) + a dependency-free node:test gate (verify.js) emitting TAP evidence, so the Validator can prove the bump on a fresh clone with no npm install.",
  "scripts": {
    "test": "node --test verify.js"
  },
  "dependencies": {
    "kleur": "4.1.4"
  }
}
